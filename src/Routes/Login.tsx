import { Box, Flex } from '@chakra-ui/react';
import { Card } from '../components/Card/Card';

export const Login = () => {
  return (
    <Box>
      <Card>
        <Flex>Login card</Flex>
      </Card>
    </Box>
  );
};
